-- AUTO RAID GUI (CLEAN VERSION)

local AutoRaid = false
local SelectedRaids = {}

local RaidList = {
    "Raid","Raid_02","Raid_03","Raid_04","Raid_Divinity","Raid_HW",
    "Raid_Christmas","Raid_Valentine","Raid_Abuse","Raid_DemonSlayer",
    "Raid_Crystal","Raid_Crazy","Raid_Insane"
}

-- GUI
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "AutoRaid"

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 250, 0, 280)
Main.Position = UDim2.new(0.5, -125, 0.5, -140)
Main.BackgroundColor3 = Color3.fromRGB(28,28,28)
Main.BorderSizePixel = 0

-- TOP BAR
local Top = Instance.new("Frame", Main)
Top.Size = UDim2.new(1,0,0,30)
Top.BackgroundColor3 = Color3.fromRGB(18,18,18)

local Title = Instance.new("TextLabel", Top)
Title.Size = UDim2.new(1,-60,1,0)
Title.Position = UDim2.new(0,10,0,0)
Title.Text = "AUTO RAID"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.TextColor3 = Color3.new(1,1,1)
Title.BackgroundTransparency = 1
Title.TextXAlignment = Enum.TextXAlignment.Left

-- CLOSE
local Close = Instance.new("TextButton", Top)
Close.Size = UDim2.new(0,30,1,0)
Close.Position = UDim2.new(1,-30,0,0)
Close.Text = "X"
Close.Font = Enum.Font.GothamBold
Close.BackgroundColor3 = Color3.fromRGB(170,0,0)
Close.TextColor3 = Color3.new(1,1,1)

Close.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- MINIMIZE
local Min = Instance.new("TextButton", Top)
Min.Size = UDim2.new(0,30,1,0)
Min.Position = UDim2.new(1,-60,0,0)
Min.Text = "-"
Min.Font = Enum.Font.GothamBold
Min.BackgroundColor3 = Color3.fromRGB(50,50,50)
Min.TextColor3 = Color3.new(1,1,1)

local Minimized = false
Min.MouseButton1Click:Connect(function()
    Minimized = not Minimized
    for _,v in pairs(Main:GetChildren()) do
        if v ~= Top then
            v.Visible = not Minimized
        end
    end
    Main.Size = Minimized and UDim2.new(0,250,0,30) or UDim2.new(0,250,0,280)
end)

-- DRAG
local UIS = game:GetService("UserInputService")
local dragging, dragStart, startPos

Top.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = Main.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- TOGGLE
local Toggle = Instance.new("TextButton", Main)
Toggle.Size = UDim2.new(0.8,0,0,35)
Toggle.Position = UDim2.new(0.1,0,0,40)
Toggle.Font = Enum.Font.GothamBold
Toggle.TextSize = 14
Toggle.TextColor3 = Color3.new(1,1,1)

local function UpdateToggle()
    Toggle.Text = AutoRaid and "AUTO: ON" or "AUTO: OFF"
    Toggle.BackgroundColor3 = AutoRaid and Color3.fromRGB(0,170,0) or Color3.fromRGB(170,0,0)
end

Toggle.MouseButton1Click:Connect(function()
    AutoRaid = not AutoRaid
    UpdateToggle()
end)

UpdateToggle()

-- SCROLL
local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(0.9,0,0,170)
Scroll.Position = UDim2.new(0.05,0,0,80)
Scroll.CanvasSize = UDim2.new(0,0,0,0)
Scroll.ScrollBarThickness = 4
Scroll.BackgroundColor3 = Color3.fromRGB(35,35,35)

local Layout = Instance.new("UIListLayout", Scroll)
Layout.Padding = UDim.new(0,4)

Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Scroll.CanvasSize = UDim2.new(0,0,0,Layout.AbsoluteContentSize.Y + 10)
end)

-- CHECKBOX LIST
for _, raidName in ipairs(RaidList) do
    local btn = Instance.new("TextButton", Scroll)
    btn.Size = UDim2.new(1,-10,0,25)
    btn.Text = "[ ] "..raidName
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 13
    btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
    btn.TextColor3 = Color3.new(1,1,1)

    btn.MouseButton1Click:Connect(function()
        if SelectedRaids[raidName] then
            SelectedRaids[raidName] = nil
            btn.Text = "[ ] "..raidName
        else
            SelectedRaids[raidName] = true
            btn.Text = "[✓] "..raidName
        end
    end)
end

-- =========================
-- AUTO JOIN (HOOK)
-- =========================

local RaidUI = require(game.ReplicatedStorage.SharedModules.RaidUIController)

local old = RaidUI.ShowInviteWindow

RaidUI.ShowInviteWindow = function(p29, data)
    if AutoRaid then
        local raidName = data and data.config and data.config.Name
        if not raidName then return end

        -- FILTER
        if next(SelectedRaids) and not SelectedRaids[raidName] then
            return
        end

        print("AUTO JOIN:", raidName)

        task.spawn(function()
            pcall(function()
                -- 🔥 ambil ROOT SYSTEM yang sama
                local root = getupvalue(old, 1)

                root.Bridge:Fire("Enemies", "Bridge", {
                    Module = "RaidSystemServer",
                    FunctionName = "Join",
                    Args = raidName
                })
            end)
        end)

        return -- skip UI
    end

    return old(p29, data)
end
