```lua
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- remove old gui
local oldGui = playerGui:FindFirstChild("AutoFarmModernGUI")
if oldGui then
	oldGui:Destroy()
end

-- remotes
local buyRemote = ReplicatedStorage.Packages.Knit.Services.MerchantService.RF.BuyItem
local chopRemote = ReplicatedStorage.Packages.Knit.Services.ChaosBreakablesService.RF.Clicked
local chestRemote = ReplicatedStorage.Packages.Knit.Services.ChestService.RF.Open
local spinRemote = ReplicatedStorage.Packages.Knit.Services.SpinnerService.RF.Spin
local giftRemote = ReplicatedStorage.Packages.Knit.Services.GiftMachineService.RF.Claim
local refineryAddRemote = ReplicatedStorage.Packages.Knit.Services.RefineryService.RF.AddAll
local refineryClaimRemote = ReplicatedStorage.Packages.Knit.Services.RefineryService.RF.Claim

local treesFolder = workspace.GameObjects.ChaosBreakables:WaitForChild("Axe")

-- states
local buyRunning=false
local chopRunning=false
local chestRunning=false
local chaosSpinRunning=false
local giftRunning=false
local refineryAddRunning=false
local refineryClaimRunning=false
local egyptSpinRunning=false

local destroyed=false

local chopDelay=0.03
local stuckTime=3

-- GUI
local gui=Instance.new("ScreenGui")
gui.Name="AutoFarmModernGUI"
gui.ResetOnSpawn=false
gui.Parent=playerGui

local frame=Instance.new("Frame")
frame.Parent=gui
frame.Size=UDim2.new(0,300,0,360)
frame.Position=UDim2.new(0,25,0.3,0)
frame.BackgroundColor3=Color3.fromRGB(20,20,24)
frame.Active=true
frame.Draggable=true

Instance.new("UICorner",frame).CornerRadius=UDim.new(0,14)

local topBar=Instance.new("Frame")
topBar.Parent=frame
topBar.Size=UDim2.new(1,0,0,38)
topBar.BackgroundColor3=Color3.fromRGB(15,15,18)
Instance.new("UICorner",topBar).CornerRadius=UDim.new(0,14)

local title=Instance.new("TextLabel")
title.Parent=topBar
title.BackgroundTransparency=1
title.Position=UDim2.new(0,14,0,0)
title.Size=UDim2.new(1,-90,1,0)
title.Text="Modern Auto Farm"
title.TextColor3=Color3.new(1,1,1)
title.Font=Enum.Font.GothamBold
title.TextSize=15
title.TextXAlignment=Enum.TextXAlignment.Left

local minimize=Instance.new("TextButton")
minimize.Parent=topBar
minimize.Size=UDim2.new(0,26,0,26)
minimize.Position=UDim2.new(1,-58,0.5,-13)
minimize.Text="—"

local close=Instance.new("TextButton")
close.Parent=topBar
close.Size=UDim2.new(0,26,0,26)
close.Position=UDim2.new(1,-28,0.5,-13)
close.Text="X"

local content=Instance.new("Frame")
content.Parent=frame
content.BackgroundTransparency=1
content.Position=UDim2.new(0,0,0,38)
content.Size=UDim2.new(1,0,1,-38)

local layout=Instance.new("UIListLayout")
layout.Parent=content
layout.Padding=UDim.new(0,6)

local function createToggle(text,callback)

	local btn=Instance.new("TextButton")
	btn.Size=UDim2.new(1,-20,0,36)
	btn.Position=UDim2.new(0,10,0,0)
	btn.BackgroundColor3=Color3.fromRGB(35,35,42)
	btn.TextColor3=Color3.new(1,1,1)
	btn.Font=Enum.Font.GothamMedium
	btn.Text=text.." : OFF"
	btn.Parent=content

	Instance.new("UICorner",btn).CornerRadius=UDim.new(0,10)

	local state=false

	btn.MouseButton1Click:Connect(function()
		state=not state
		btn.Text=text.." : "..(state and "ON" or "OFF")
		callback(state)
	end)

end

createToggle("Auto Buy Atomic",function(v) buyRunning=v end)
createToggle("Auto Chop Axe",function(v) chopRunning=v end)
createToggle("Auto Atomic Chest",function(v) chestRunning=v end)
createToggle("Auto Chaos Spin",function(v) chaosSpinRunning=v end)
createToggle("Auto Gift Claim",function(v) giftRunning=v end)
createToggle("Auto Refinery Add",function(v) refineryAddRunning=v end)
createToggle("Auto Refinery Claim",function(v) refineryClaimRunning=v end)
createToggle("Auto Egyptian Spin",function(v) egyptSpinRunning=v end)

close.MouseButton1Click:Connect(function()
	destroyed=true
	gui:Destroy()
end)

minimize.MouseButton1Click:Connect(function()
	content.Visible=not content.Visible
end)

-- get tree
local function getNearestTree()

	local char=player.Character
	if not char then return end

	local root=char:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local nearest=nil
	local dist=math.huge

	for _,tree in pairs(treesFolder:GetChildren()) do
		if tree:GetAttribute("Destroyed")~=true then
			local bound=tree:FindFirstChild("XZBounds")
			if bound then
				local d=(root.Position-bound.Position).Magnitude
				if d<dist then
					dist=d
					nearest=bound
				end
			end
		end
	end

	return nearest
end

local function moveToTree(bound)

	local char=player.Character
	if not char then return end

	local root=char:FindFirstChild("HumanoidRootPart")
	if not root then return end

	local dir=(root.Position-bound.Position).Unit
	local pos=bound.Position+dir*6+Vector3.new(0,2,0)

	root.AssemblyLinearVelocity=Vector3.zero
	root.AssemblyAngularVelocity=Vector3.zero

	char:PivotTo(CFrame.new(pos,bound.Position))

end

-- main loop
task.spawn(function()

	while not destroyed do

		if buyRunning then
			for i=1,13 do
				pcall(function()
					buyRemote:InvokeServer("Atomic",i)
				end)
				task.wait(0.15)
			end
		end

		if chopRunning then

			local bound=getNearestTree()

			if bound then

				moveToTree(bound)

				task.wait(0.15)

				local tree=bound.Parent
				local start=tick()

				repeat

					pcall(function()
						chopRemote:InvokeServer(bound)
					end)

					task.wait(chopDelay)

				until tree:GetAttribute("Destroyed")==true
				or tick()-start>stuckTime

			end
		end

		if chestRunning then
			pcall(function()
				chestRemote:InvokeServer("AtomicChest")
			end)
		end

		if chaosSpinRunning then
			pcall(function()
				spinRemote:InvokeServer("Chaos Fortune","x25")
			end)
		end

		if giftRunning then
			pcall(function()
				giftRemote:InvokeServer("23")
			end)
		end

		if refineryAddRunning then
			pcall(function()
				refineryAddRemote:InvokeServer("WildWest")
			end)
		end

		if refineryClaimRunning then
			pcall(function()
				refineryClaimRemote:InvokeServer("WildWest")
			end)
		end

		if egyptSpinRunning then
			pcall(function()
				spinRemote:InvokeServer("Egyptian Fortune","x25")
			end)
		end

		task.wait(1)

	end

end)
```
