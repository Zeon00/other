--// SERVICES
local VirtualUser = game:GetService("VirtualUser")
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")


game.Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

local player = Players.LocalPlayer

--// LIB
local Library = require(RS:WaitForChild("Framework"):WaitForChild("Library"))
local Remote = Library.Remote
local MultiplierService = Library:GetService("MultiplierService")

--// SETTINGS
getgenv().Aura = false
getgenv().TP = false
getgenv().AutoClaim = false
getgenv().AutoRaid = false
getgenv().AutoClosest = false

local EnemyFolder = workspace:WaitForChild("_ENEMIES"):WaitForChild("Client")
local ScrollRemote = RS:WaitForChild("ffrostflame_bridgenet2@1.0.0"):WaitForChild("dataRemoteEvent")

--// FASTEST NEAREST
local function GetNearest()
    local char = player.Character
    if not char then return end

    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local nearest, dist = nil, math.huge

    for _, mob in ipairs(EnemyFolder:GetChildren()) do
        local hrp = mob:FindFirstChild("HumanoidRootPart")
        if hrp then
            local d = (hrp.Position - root.Position).Magnitude
            if d < dist then
                dist = d
                nearest = hrp
            end
        end
    end

    return nearest
end

--// FAST TP
RunService.RenderStepped:Connect(function()
    if not getgenv().TP then return end

    local char = player.Character
    if not char then return end

    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local target = GetNearest()
    if target then
        root.CFrame = target.CFrame * CFrame.new(0,0,3)
    end
end)

--// ULTRA FAST AURA
task.spawn(function()
    while true do
        if getgenv().Aura then

            for i = 1,10 do
                task.spawn(function()
                    pcall(function()
                        Remote:Fire("ClickSystem","Execute",Library.Target)
                    end)
                end)
            end

            for i = 1,10 do
                task.spawn(function()
                    pcall(function()
                        Remote:Fire("WarriorSystem","Attack",Library.Target, true)
                    end)
                end)
            end

pcall(function()
    local hum = player.Character and player.Character:FindFirstChild("Humanoid")
    if hum then
        hum.WalkSpeed = 75
    end
end)

            pcall(function()
                local hum = player.Character and player.Character:FindFirstChild("Humanoid")
                if hum then
                    hum.WalkSpeed = 75
                end
            end)
        end

        task.wait()
    end
end)

--// AUTO RAID
task.spawn(function()
    while task.wait(10) do
        if getgenv().AutoRaid then
            pcall(function()

                -- CREATE RAID
                local args = {
                    {
                        {
                            "GamemodeSystem",
                            "Create",
                            "Elemental Mode",
                            "WindRaid",
                            "Easy",
                            n = 6
                        },
                        "\002"
                    }
                }

                ScrollRemote:FireServer(unpack(args))

                task.wait(2)

                -- START RAID
                local args2 = {
                    {
                        {
                            "GamemodeSystem",
                            "Start",
                            "Elemental Mode",
                            8594485805,
                            n = 4
                        },
                        "\002"
                    }
                }

                ScrollRemote:FireServer(unpack(args2))
            end)
        end
    end
end)

--// AUTO CLOSEST
local ClosestIDs = {
    "3f35c4977dcd480e850543f5e831c178",
    "3d07267fe51e4fba895e38458cc4b868",
    "22d0716260164b7ebdd55a134723544b",
    "2812a9cb0e1a47d59bed2829e29f86c8",
    "7a2e9d45427a4586b7881b9004749c77",
    "bb1387ac35004b06b96b07e57f5d9b74",
    "67f1aa8cf8a54ed18c97a2071cebf314",
    "f600f2460d254be1b089e9e8a0de55e3"
}

local ServerEnemies = workspace:WaitForChild("_ENEMIES"):WaitForChild("Server")

local function GetEnemyFolder()
    for _, folder in pairs(ServerEnemies:GetChildren()) do
        if #folder:GetChildren() > 0 then
            return folder
        end
    end
end

task.spawn(function()
    while task.wait() do
        if getgenv().AutoClosest then

            local mobFolder = GetEnemyFolder()

            if mobFolder then
                local targetMob = mobFolder:GetChildren()[1]

                if targetMob then

                    local args = {{}}

                    for _, id in ipairs(ClosestIDs) do
                        table.insert(args[1], {
                            "WarriorSystem",
                            "Closest",
                            id,
                            n = 3
                        })

                        table.insert(args[1], "\002")
                    end

                    for i = 1,10 do
                        task.spawn(function()
                            pcall(function()
                                ScrollRemote:FireServer(unpack(args))
                            end)
                        end)
                    end

                end
            end
        end
    end
end)

--// GUI
pcall(function()
    game.CoreGui.FastAuraHub:Destroy()
end)

local gui = Instance.new("ScreenGui")
gui.Name = "FastAuraHub"
gui.Parent = game.CoreGui

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0,220,0,315)
main.Position = UDim2.new(1,-230,0.35,0)
main.BackgroundColor3 = Color3.fromRGB(20,20,20)
main.Active = true
main.Draggable = true
Instance.new("UICorner", main)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1,-70,0,30)
title.Position = UDim2.new(0,10,0,0)
title.BackgroundTransparency = 1
title.Text = "Fast Aura"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.TextXAlignment = Enum.TextXAlignment.Left

local close = Instance.new("TextButton", main)
close.Size = UDim2.new(0,30,0,30)
close.Position = UDim2.new(1,-35,0,0)
close.Text = "X"
close.BackgroundColor3 = Color3.fromRGB(180,40,40)
close.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", close)

close.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

--// MINIMIZE
local mini = Instance.new("TextButton", main)
mini.Size = UDim2.new(0,30,0,30)
mini.Position = UDim2.new(1,-70,0,0)
mini.Text = "-"
mini.BackgroundColor3 = Color3.fromRGB(70,70,70)
mini.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", mini)

local openBtn = Instance.new("TextButton", gui)
openBtn.Size = UDim2.new(0,50,0,50)
openBtn.Position = UDim2.new(1,-60,0.5,0)
openBtn.Text = "OPEN"
openBtn.Visible = false
openBtn.BackgroundColor3 = Color3.fromRGB(25,25,25)
openBtn.TextColor3 = Color3.new(1,1,1)
openBtn.Active = true
openBtn.Draggable = true
Instance.new("UICorner", openBtn)

mini.MouseButton1Click:Connect(function()
    main.Visible = false
    openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
    main.Visible = true
    openBtn.Visible = false
end)

--// TOGGLE
local function Toggle(text, y, callback)
    local state = false

    local btn = Instance.new("TextButton", main)
    btn.Size = UDim2.new(1,-20,0,35)
    btn.Position = UDim2.new(0,10,0,y)
    btn.BackgroundColor3 = Color3.fromRGB(45,45,45)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.Text = text.." OFF"

    Instance.new("UICorner", btn)

    btn.MouseButton1Click:Connect(function()
        state = not state

        btn.Text = text.." "..(state and "ON" or "OFF")
        btn.BackgroundColor3 = state
            and Color3.fromRGB(0,170,0)
            or Color3.fromRGB(45,45,45)

        callback(state)
    end)
end

Toggle("Aura",40,function(v)
    getgenv().Aura = v
end)

Toggle("TP",85,function(v)
    getgenv().TP = v
end)

Toggle("Auto Claim",130,function(v)
    getgenv().AutoClaim = v
end)

Toggle("Auto Closest",175,function(v)
    getgenv().AutoClosest = v
end)

Toggle("Auto Raid",220,function(v)
    getgenv().AutoRaid = v
end)

--// ANTI AFK
task.spawn(function()
    while task.wait(10) do
        pcall(function()
            local hum = player.Character and player.Character:FindFirstChild("Humanoid")
            if hum then
                hum.Jump = true
            end
        end)
    end
end)
