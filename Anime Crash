--// SERVICES
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer
local Remote = ReplicatedStorage:WaitForChild("BridgeNet2"):WaitForChild("dataRemoteEvent")

--// STATE
local AutoAttack = false
local AutoTP = false
local AutoRaid = false
local AutoJoin = false
local AutoTrialEasy = false
local AutoTrialMedium = false
local AutoGacha = false 

--// GUI
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "AutoFarmGUI"
gui.ResetOnSpawn = false

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 260, 0, 350)
main.Position = UDim2.new(0.5, -130, 0.5, -110)
main.BackgroundColor3 = Color3.fromRGB(20,20,20)
main.Active = true
main.Draggable = true
Instance.new("UICorner", main)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1,0,0,30)
title.BackgroundTransparency = 1
title.Text = "AUTO FARM V4"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 14

local close = Instance.new("TextButton", main)
close.Size = UDim2.new(0,30,0,30)
close.Position = UDim2.new(1,-30,0,0)
close.Text = "X"
close.BackgroundTransparency = 1
close.TextColor3 = Color3.fromRGB(255,80,80)

local mini = Instance.new("TextButton", main)
mini.Size = UDim2.new(0,30,0,30)
mini.Position = UDim2.new(1,-60,0,0)
mini.Text = "-"
mini.BackgroundTransparency = 1
mini.TextColor3 = Color3.fromRGB(200,200,200)

local icon = Instance.new("TextButton", gui)
icon.Size = UDim2.new(0,50,0,50)
icon.Position = UDim2.new(0,20,0.5,0)
icon.Text = "OPEN"
icon.Visible = false
icon.BackgroundColor3 = Color3.fromRGB(30,30,30)
Instance.new("UICorner", icon)

mini.MouseButton1Click:Connect(function()
	main.Visible = false
	icon.Visible = true
end)

icon.MouseButton1Click:Connect(function()
	main.Visible = true
	icon.Visible = false
end)

close.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

--// BUTTON
local function createToggle(name, y)
	local btn = Instance.new("TextButton", main)
	btn.Size = UDim2.new(0.9,0,0,35)
	btn.Position = UDim2.new(0.05,0,0,y)
	btn.BackgroundColor3 = Color3.fromRGB(40,40,40)
	btn.TextColor3 = Color3.new(1,1,1)
	btn.Font = Enum.Font.Gotham
	btn.TextSize = 13
	btn.Text = name.." : OFF"
	Instance.new("UICorner", btn)
	return btn
end

local atkBtn = createToggle("Auto Attack", 40)
local tpBtn = createToggle("Auto TP", 85)
local joinBtn = createToggle("Auto Join", 130)
local raidBtn = createToggle("Auto Raid", 175)
local trialEasyBtn = createToggle("Trial Easy", 220)
local trialMedBtn = createToggle("Trial Medium", 265)
local gachaBtn = createToggle("Auto Gacha", 310)

gachaBtn.MouseButton1Click:Connect(function()
	AutoGacha = not AutoGacha
	gachaBtn.Text = "Auto Gacha : "..(AutoGacha and "ON" or "OFF")
end)

atkBtn.MouseButton1Click:Connect(function()
	AutoAttack = not AutoAttack
	atkBtn.Text = "Auto Attack : "..(AutoAttack and "ON" or "OFF")
end)

tpBtn.MouseButton1Click:Connect(function()
	AutoTP = not AutoTP
	tpBtn.Text = "Auto TP : "..(AutoTP and "ON" or "OFF")
end)

joinBtn.MouseButton1Click:Connect(function()
	AutoJoin = not AutoJoin
	joinBtn.Text = "Auto Join : "..(AutoJoin and "ON" or "OFF")
end)

raidBtn.MouseButton1Click:Connect(function()
	AutoRaid = not AutoRaid
	raidBtn.Text = "Auto Raid : "..(AutoRaid and "ON" or "OFF")
end)

trialEasyBtn.MouseButton1Click:Connect(function()
	AutoTrialEasy = not AutoTrialEasy
	trialEasyBtn.Text = "Trial Easy : "..(AutoTrialEasy and "ON" or "OFF")
end)

trialMedBtn.MouseButton1Click:Connect(function()
	AutoTrialMedium = not AutoTrialMedium
	trialMedBtn.Text = "Trial Medium : "..(AutoTrialMedium and "ON" or "OFF")
end)
--// TARGET SYSTEM (FIXED)
local overlap = OverlapParams.new()
overlap.FilterType = Enum.RaycastFilterType.Include
overlap.FilterDescendantsInstances = {
	workspace.Game.Mobs,
	workspace.Game.Rooms,
	workspace.Game.Trials
}

local function getTargets()
	local char = player.Character
	if not char or not char:FindFirstChild("HumanoidRootPart") then return {} end
	
	local hrp = char.HumanoidRootPart

	local range = 10
	pcall(function()
		range = require(ReplicatedStorage.GameCore).Player.State.Get("Range") or 10
	end)

	local parts = workspace:GetPartBoundsInRadius(hrp.Position, range / 2, overlap)

	local targets = {}
	local used = {}

	for _, part in pairs(parts) do
		local model = part.Parent
		if not used[model] and part:GetAttribute("Enemy") and not part:GetAttribute("Died") then
			table.insert(targets, part)
			used[model] = true
		end
	end

	return targets
end

--// AUTO ATTACK (FIXED STABLE)
task.spawn(function()
	while true do
		if AutoAttack then
			local targets = getTargets()

			if #targets > 0 then
				-- HIT MUSUH
				Remote:FireServer({
					{
						"Action",
						"Action",
						{
							Callback = "M1",
							Action = "Callback",
							Targets = targets
						}
					},
					"\005"
				})
			else
				-- GA ADA MUSUH → TETEP CLICK
				Remote:FireServer({
					{
						"Action",
						"Action",
						{
							Callback = "M1",
							Action = "Callback",
							Targets = {}
						}
					},
					"\005"
				})
			end

			task.wait(0.05)
		else
			task.wait(0.05)
		end
	end
end)

task.spawn(function()
	while true do
		if AutoRaid then
			Remote:FireServer({
				{
					"Gamemodes",
					"Create",
					{
						Raid = "Frieza Force Invasion",
						Gamemode = "Raid",
						Mode = "Medium"
					}
				},
				"\005"
			})

			task.wait(5) -- delay biar ga spam error
		else
			task.wait(1)
		end
	end
end)
--// AUTO TP (BIARIN SESUAI LU)
RunService.Heartbeat:Connect(function()
	if AutoTP and not AutoGacha then
		local char = player.Character
		if not char or not char:FindFirstChild("HumanoidRootPart") then return end
		
		local hrp = char.HumanoidRootPart

		for _, v in pairs(workspace.Game.Mobs.Client:GetDescendants()) do
			if v:IsA("BasePart") and v.Name == "HumanoidRootPart" then
				hrp.CFrame = v.CFrame * CFrame.new(0,2,2)
				break
			end
		end
	end
end)

task.spawn(function()
	while true do
		if AutoJoin then
			pcall(function()
				local rooms = workspace.Game:FindFirstChild("Rooms")

				if rooms then
					local room = rooms:FindFirstChildWhichIsA("Folder")

					if room then
						Remote:FireServer({
							{
								"Gamemodes",
								"Join",
								{
									Gamemode = "Raid",
									Room = room.Name
								}
							},
							"\005"
						})
					end
				end
			end)

			task.wait(2)
		else
			task.wait(1)
		end
	end
end)

task.spawn(function()
	while true do
		
		if AutoTrialEasy then
			Remote:FireServer({
				{
					"TrialReceiver",
					"Join",
					"Easy"
				},
				"\005"
			})
			task.wait(2)
		end

		if AutoTrialMedium then
			Remote:FireServer({
				{
					"TrialReceiver",
					"Join",
					"Medium"
				},
				"\005"
			})
			task.wait(2)
		end

		task.wait(1)
	end
end)
task.spawn(function()
	while true do
		if AutoGacha then
			pcall(function()
				local args = {
					{
						{
							"Pets",
							"Summon",
							{
								useMaxRoll = true
							}
						},
						"\005"
					}
				}
				game:GetService("ReplicatedStorage")
					:WaitForChild("BridgeNet2")
					:WaitForChild("dataRemoteEvent")
					:FireServer(unpack(args))
			end)

			task.wait(0.6) -- 🔥 jangan terlalu kecil
		else
			task.wait(1)
		end
	end
end)
--// ANTI AFK
player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)
