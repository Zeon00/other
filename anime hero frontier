--// DESTROY GUI LAMA
pcall(function()
    game.CoreGui.ModernFarmGUI:Destroy()
end)

--// SERVICES
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer

--// REMOTE
local ClickRemote = RS:WaitForChild("RemotesFolder"):WaitForChild("Click")

--// STATE
local AutoClick = false
local AutoTP = false

--// GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ModernFarmGUI"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- MAIN
local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0, 300, 0, 220)
Main.Position = UDim2.new(0.5, -150, 0.5, -110)
Main.BackgroundColor3 = Color3.fromRGB(20,20,20)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0,16)

local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(80,80,80)
Stroke.Thickness = 1.5

-- TITLE
local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Modern Farm GUI"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.TextColor3 = Color3.fromRGB(255,255,255)

-- MINIMIZE
local MinBtn = Instance.new("TextButton")
MinBtn.Parent = Main
MinBtn.Size = UDim2.new(0,30,0,30)
MinBtn.Position = UDim2.new(1,-70,0,5)
MinBtn.Text = "-"
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 20
MinBtn.BackgroundColor3 = Color3.fromRGB(45,45,45)
MinBtn.TextColor3 = Color3.fromRGB(255,255,255)
MinBtn.BorderSizePixel = 0
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(1,0)

-- CLOSE
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = Main
CloseBtn.Size = UDim2.new(0,30,0,30)
CloseBtn.Position = UDim2.new(1,-35,0,5)
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 16
CloseBtn.BackgroundColor3 = Color3.fromRGB(255,60,60)
CloseBtn.TextColor3 = Color3.fromRGB(255,255,255)
CloseBtn.BorderSizePixel = 0
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(1,0)

-- AUTO CLICK
local ClickToggle = Instance.new("TextButton")
ClickToggle.Parent = Main
ClickToggle.Size = UDim2.new(0,240,0,45)
ClickToggle.Position = UDim2.new(0.5,-120,0,65)
ClickToggle.BackgroundColor3 = Color3.fromRGB(40,40,40)
ClickToggle.Text = "AUTO CLICK : OFF"
ClickToggle.Font = Enum.Font.GothamBold
ClickToggle.TextSize = 15
ClickToggle.TextColor3 = Color3.fromRGB(255,255,255)
ClickToggle.BorderSizePixel = 0
Instance.new("UICorner", ClickToggle).CornerRadius = UDim.new(0,12)

-- AUTO TP
local TPToggle = Instance.new("TextButton")
TPToggle.Parent = Main
TPToggle.Size = UDim2.new(0,240,0,45)
TPToggle.Position = UDim2.new(0.5,-120,0,120)
TPToggle.BackgroundColor3 = Color3.fromRGB(40,40,40)
TPToggle.Text = "AUTO TP : OFF"
TPToggle.Font = Enum.Font.GothamBold
TPToggle.TextSize = 15
TPToggle.TextColor3 = Color3.fromRGB(255,255,255)
TPToggle.BorderSizePixel = 0
Instance.new("UICorner", TPToggle).CornerRadius = UDim.new(0,12)

-- STATUS
local Status = Instance.new("TextLabel")
Status.Parent = Main
Status.Size = UDim2.new(1,0,0,25)
Status.Position = UDim2.new(0,0,1,-30)
Status.BackgroundTransparency = 1
Status.Text = "Ready"
Status.Font = Enum.Font.Gotham
Status.TextSize = 14
Status.TextColor3 = Color3.fromRGB(180,180,180)

-- MINI ICON
local MiniIcon = Instance.new("TextButton")
MiniIcon.Parent = ScreenGui
MiniIcon.Size = UDim2.new(0,55,0,55)
MiniIcon.Position = UDim2.new(0,20,0.5,-25)
MiniIcon.BackgroundColor3 = Color3.fromRGB(25,25,25)
MiniIcon.Text = "GUI"
MiniIcon.Font = Enum.Font.GothamBold
MiniIcon.TextSize = 15
MiniIcon.TextColor3 = Color3.fromRGB(255,255,255)
MiniIcon.Visible = false
MiniIcon.BorderSizePixel = 0
Instance.new("UICorner", MiniIcon).CornerRadius = UDim.new(1,0)

-- UPDATE BUTTONS
local function UpdateButtons()
    ClickToggle.Text = "AUTO CLICK : "..(AutoClick and "ON" or "OFF")
    ClickToggle.BackgroundColor3 = AutoClick and Color3.fromRGB(0,170,90) or Color3.fromRGB(40,40,40)

    TPToggle.Text = "AUTO TP : "..(AutoTP and "ON" or "OFF")
    TPToggle.BackgroundColor3 = AutoTP and Color3.fromRGB(0,170,90) or Color3.fromRGB(40,40,40)
end

UpdateButtons()

-- TOGGLES
ClickToggle.MouseButton1Click:Connect(function()
    AutoClick = not AutoClick
    UpdateButtons()
end)

TPToggle.MouseButton1Click:Connect(function()
    AutoTP = not AutoTP
    UpdateButtons()
end)

-- MINIMIZE
MinBtn.MouseButton1Click:Connect(function()
    Main.Visible = false
    MiniIcon.Visible = true
end)

MiniIcon.MouseButton1Click:Connect(function()
    Main.Visible = true
    MiniIcon.Visible = false
end)

-- DESTROY
CloseBtn.MouseButton1Click:Connect(function()
    AutoClick = false
    AutoTP = false
    ScreenGui:Destroy()
end)

-- GET NEAREST ENEMY
local function GetNearestEnemy()
    local character = player.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")

    if not hrp then
        return nil
    end

    local closest
    local distance = math.huge

    local enemiesFolder = workspace:FindFirstChild("Enemies")
        and workspace.Enemies:FindFirstChild("Character")

    if not enemiesFolder then
        return nil
    end

    for _, enemy in pairs(enemiesFolder:GetChildren()) do
        local enemyHRP = enemy:FindFirstChild("HumanoidRootPart")
        local humanoid = enemy:FindFirstChildOfClass("Humanoid")

        if enemyHRP and humanoid and humanoid.Health > 0 then
            local mag = (hrp.Position - enemyHRP.Position).Magnitude

            if mag < distance then
                distance = mag
                closest = enemy
            end
        end
    end

    return closest
end

-- AUTO CLICK LOOP
task.spawn(function()
    while task.wait() do
        if AutoClick then
            pcall(function()
                for i = 1,10 do
                    task.spawn(function()
                        ClickRemote:FireServer()
                    end)
                end
            end)
        end
    end
end)

-- AUTO TP LOOP
task.spawn(function()
    while task.wait(0.15) do
        if AutoTP then
            pcall(function()
                local enemy = GetNearestEnemy()

                if enemy then
                    local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                    local enemyHRP = enemy:FindFirstChild("HumanoidRootPart")

                    if hrp and enemyHRP then
                        hrp.CFrame = enemyHRP.CFrame * CFrame.new(0,0,3)
                        Status.Text = "Target : "..enemy.Name
                    end
                else
                    Status.Text = "No Enemy"
                end
            end)
        else
            Status.Text = "Idle"
        end
    end
end)
