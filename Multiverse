--// SERVICES
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local MonsterRemote = RS:WaitForChild("Remotes"):WaitForChild("MonsterEvent")
local TaskRemote = RS:WaitForChild("Remotes"):WaitForChild("DataInterface"):WaitForChild("Task")
local SkillRemote = RS:WaitForChild("Remotes"):WaitForChild("SkillEvent")

-- AUTO ON
local ON = true

--// GET TARGET
local function getTarget()
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local room = char.Parent and char.Parent.Parent
    if not room then return end

    local folder = room:FindFirstChild("Monster_")
    if not folder then return end

    local closest, dist = nil, math.huge

    for _,mob in pairs(folder:GetChildren()) do
        local mhrp = mob:FindFirstChild("HumanoidRootPart")
        if mhrp then
            local d = (hrp.Position - mhrp.Position).Magnitude
            if d < dist then
                dist = d
                closest = mob
            end
        end
    end

    return closest
end

--// DPS LOOP
task.spawn(function()
    local delay = 0.04

    while true do
        if ON then
            local target = getTarget()

            if target then
                local success = 0

                for i = 1,2 do
                    local ok = pcall(function()
                        MonsterRemote:FireServer("DamToMonster", target, {
                            damtype = "normal"
                        })
                    end)

                    if ok then
                        success = success + 1
                    end
                end

                -- auto adjust
                if success < 2 then
                    delay = math.clamp(delay + 0.005, 0.03, 0.08)
                else
                    delay = math.clamp(delay - 0.002, 0.03, 0.08)
                end
            end
        end

        task.wait(delay)
    end
end)

--// 🔥 ANTI AFK REAL (INI KUNCI)
task.spawn(function()
    while true do
        pcall(function()
            -- fake attack (dianggap aktif)
            TaskRemote:FireServer("slash")

            -- fake skill (biar lebih legit)
            SkillRemote:FireServer("skillRequest", {
                pos = "E",
                _tick = tick()
            })
        end)

        task.wait(25) -- interval aman
    end
end)

--// GERAK DIKIT (SUPPORT)
task.spawn(function()
    while true do
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")

        if hrp then
            hrp.CFrame = hrp.CFrame * CFrame.new(0.2,0,0)
            task.wait(0.2)
            hrp.CFrame = hrp.CFrame * CFrame.new(-0.2,0,0)
        end

        task.wait(20)
    end
end)
